#ifndef ITERADORVENDEDORES_H_INCLUDED
#define ITERADORVENDEDORES_H_INCLUDED
#include "Vendedor.h"
/*
class IteradorVendedores
{
private:
    typedef struct nodoV
    {
        Vendedor info;
        nodoV *sig;
    } NodoV;
    typedef struct
    {
        NodoV * prim;
        NodoV * ult;
    } LPPFVendedores;
public:
    IteradorVendedores (); // Constructor por defecto
    void insertarVendedor (Vendedor); //Inserta la nueva estadía al iterador
    //Precondición: el iterador no está lleno
    bool hayMasVendedores (); //Indica si quedan más Estadías por recorrer
    Vendedor proximoVendedor (); //Devuelve la próxima estadía por recorrer
    //Precondición: hay más estadías por recorrer
    bool estaLlenoVendedores (); //Indica si el iterador está lleno ///PREGUNTAR SI APLICA AL SER LPPF
};
*/
#endif // ITERADORVENDEDORES_H_INCLUDED
