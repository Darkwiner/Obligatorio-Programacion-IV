#ifndef ITERADORSUPERVISORES_H_INCLUDED
#define ITERADORSUPERVISORES_H_INCLUDED
#include "Supervisor.h"
/*
class IteradorSupervisores
{
private:
    typedef struct nodoS
    {
        Supervisor info;
        nodoS *sig;
    } NodoS;
    typedef struct
    {
        NodoS * prim;
        NodoS * ult;
    } LPPFSupervisores;
public:
    IteradorSupervisores (); //Constructor por defecto
    void insertarSupervisor (Supervisor); //Inserta la nueva estadía al iterador
    //Precondición: el iterador no está lleno
    bool hayMasSupervisores (); //Indica si quedan más Estadías por recorrer
    Supervisor proximoSupervisor (); //Devuelve la próxima estadía por recorrer
    //Precondición: hay más estadías por recorrer
    bool estaLlenoSupervisores (); //Indica si el iterador está lleno ///PREGUNTAR SI APLICA AL SER LPPF
};
*/
#endif // ITERADORSUPERVISORES_H_INCLUDED
